# Auth integrated pack (frontend only)

- Login / Register / Forgot password with OTP (mock)
- OTP is stored in localStorage and logged to console. Replace with real email service in production.
- Session is stored in localStorage. If "Ghi nhớ" unchecked, session will be cleared when the tab closes.

Drop-in replacement for: src/pages/AuthPage.jsx
