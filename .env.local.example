# .env.local (hoặc set trên Vercel → Project → Settings → Environment Variables)
# Điền URL src của iframe biểu đồ Moniq (chỉ phần src, KHÔNG cả tag <iframe>)
VITE_MONIQ_CHART_URL=https://your-moniq-iframe-src-here
